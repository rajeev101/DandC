<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Your Page Title</title>
    <style type="text/css">
        .container {
            background-color: rgba(127, 199, 217, 0.28);
            margin-top: -50px;
            padding: 20px;
        }
    </style>
</head>
<body>
    <%= render 'navbar' %>
    <div class="container">
        <% @course.syllabuses.each do |syllabus| %>
            <h1 style="font-weight: bold;"><%= syllabus.title %></h1>
            <p><%= raw syllabus.description %></p>
        <% end %>
    </div>
    <%= render 'footer' %>
</body>
</html>
